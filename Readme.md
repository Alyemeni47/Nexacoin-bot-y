# Solana Flask Web Server

A lightweight Flask server to fetch Solana wallet balances.

## Features
- Get SOL balance for any wallet via `/balance?wallet=<address>`.
- Ready for deployment (Render, Heroku, etc.).

## Setup

1. **Clone the repo**
   ```bash
   git clone https://github.com/yourusername/solana-flask-server.git
   cd solana-flask-server
